% ======================================================================= %
% Draw area plot of -sin(x)/x;
% ======================================================================= %
clc; clear; close all;

x = linspace(-3*pi, 3*pi, 200);
y = -sin(x)./x;
area(x, y);
title('Area plot');